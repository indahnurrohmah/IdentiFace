name: Deploy AI to Azure B2

on:
  push:
    branches:
      - main
    paths:
      - 'face-attendance/**'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Log in to Azure Container Registry
      uses: azure/docker-login@v1
      with:
        login-server:  identifaceacr.azurecr.io
        username: ${{ secrets.REGISTRY_USERNAME }}
        password: ${{ secrets.REGISTRY_PASSWORD }}

    - name: Build and Push Docker Image
      run: |
        # Masuk ke dalam folder monorepo kamu
        cd face-attendance
        
        # Merakit dan Mengunggah Wadah
        # GANTI 'namaregistrymu' DENGAN NAMA ACR MILIKMU
        docker build -t namaregistrymu.azurecr.io/identiface-ai:latest .
        docker push namaregistrymu.azurecr.io/identiface-ai:latest

    - name: Deploy to Azure Web App
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'identiface-ai-api'
        publish-profile: ${{ secrets.AZURE_AI_WEBAPP_PUBLISH_PROFILE }}
        images: ' identifaceacr.azurecr.io/identiface-ai:latest'